# spring:
#   datasource:
#     url: jdbc:postgresql://172.18.0.2:5432/postgres
#     username: postgres
#     password: root
#     driver-class-name: org.postgresql.Driver
#     # Increase the connection timeout
#     hikari:
#       connection-timeout: 60000  # 60 seconds timeout (default is 30000)
#       validation-timeout: 5000
#   jpa:
#     hibernate:
#       ddl-auto: update
#     properties:
#       hibernate:
#         dialect: org.hibernate.dialect.PostgreSQLDialect
#   flyway:
#     enabled: false  # Disable Flyway if you're not using it for migrations
 
